<Window x:Class="Login.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Login"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="1" x:Name="EmailLabel" FontSize="50">

        </Label>

        <Button Grid.Column="0" Grid.Row="1" Height="40" VerticalAlignment="Top">
            Change photo
        </Button>
        <TextBox Grid.Column="1" Grid.Row="1" Margin="0,20,0,0" x:Name="description">
            
        </TextBox>
        <Button Grid.Column="1" Grid.Row="1" Height="20" VerticalAlignment="Top" Click="DeskChange">
            Change description
        </Button>

    </Grid>
    
</Window>
